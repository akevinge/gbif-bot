import Jimp from "jimp";
import { cachedMapData } from "./MapData";

export const generateOccMap = ({ taxonKey }: { taxonKey: string | number }) => {
  console.log(cachedMapData.easternHemisphereMapData);
};
